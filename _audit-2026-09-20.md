# 全仓审查报告 · 2026-09-20

> 任务原话：**「遍历全文与markdown还有记忆与其他记录，检查是否有bug」**
> 范围：`saki.html`（24 914 行）· `markdown/`（14 份）· `.workbuddy-ai/memory/`（5 份）·
> 用户级技能 `verify-single-file-html-app` · 两套 `_verify/` 目录。
>
> ⚠ **`saki.html` 一行未改**（sha256 前 16 位 `fb440e68c5746aaf`，1 275 998 字节，mtime 未变）。
> 本报告是**一次性**的，结论已折进 `markdown/CHANGELOG.md` 的「2026-09-20（续四）」。

---

## 0. 结论一句话

**产品没有 bug。** 找到的是 9 处文档漂移、3 处**还挂在现行文档里的旧结论**、
一个**已修好的 14.5 GB 验证基建泄漏**，以及 4 项遗留（当时都标了「等你拍板」）。

> ✅ **同一天全部收口**（2026-09-20 第十~十二轮）：4.1 两份 `blk0.js` 已删 ·
> 4.2 泄漏已修、943 个残留也已清（13.13 GB）· 4.3 三处死声明已删 ·
> 4.4 memo 静默截断已修。**这份报告现在可以当存档读了** —— 每项下面都有 ✅ 收口记录。

---

## 1. 基线：两套套件先跑，别猜

| 套件 | 结果 |
|---|---|
| 本地 12 套（`G:\saki\_verify\run-all.js`） | **1664 / 0** |
| 外部 8 层（`…2026-09-17-00-10-39\_verify\run-all.js`） | 7 套断言 **1628 / 0** + `静态检查：全部通过` |
| `check.js` 快路径 | 5 / 0（`937` 声明 · `139` 内联 handler · `766` 个 `ST_`·`st` 名字） |

改完**重跑过**（不是沿用旧结论），两套与基线逐条一致。
另外验证了 CDP 临时目录：连跑两次全量，`cdp-*` **净增 0**（修之前是每次 +11）。

---

## 2. 已修：9 处文档漂移

| # | 文件 | 原来 | 实际 |
|---|---|---|---|
| 1 | `README.md` §2 body 顶层区块表 | 4609 / 4628 / 4654 / 4899 / 4938 / 4963 / 4968 / 4973 | **整表后移 262 行** → 4871 / 4890 / 4916 / 5161 / 5200→5201 / 5225 / 5230 / 5235 |
| 2 | `README.md` §7 `blk0.js` | 「约 1 049 KB」 | 删掉数字（**生成物**，大小跟着 `saki.html` 走） |
| 3 | `04-card-editor.md` | 「覆盖 45 扩到 **758**」×2 | **766**，并注明「这个数会一直涨，要就现跑 `check.js`」 |
| 4 | `04-card-editor.md` | 改写要求「**12 个**预设 chip」×2 | **14**（`ST_AI_REQ_PRESETS` 实为 14） |
| 5 | `04-card-editor.md` | 把一次性探针 `_verify/_probe-mine.js` 当**现存文件**引用 | 标明「一次性，跑完已删」 |
| 6 | `MEMORY.md` | 「19 个工具 = 13 改卡 + fetch_url/set_tasks/remember + 文档与条目各 3」 | **算式不成立**（13+3+6=22≠19）→ 重写分解 |
| 7 | `RULES.md` | 「`remember`，**13 个改卡工具之后**的第 3 个」 | `remember` 是**第 13 个**工具（不是第 16 个） |
| 8 | `SKILL.md` | cross-validate 的例子写 `758` | `766` + 新增一节「**记成快照，不是常量**」 |
| 9 | `README.md` §7 | 缺 `blk0.js` 过时 / CDP 泄漏两块告警 | 已补 |

**核对无误、不用动的**（都是枚举出来的，不是抽查）：
`README` §3 的 47 个 CSS 锚点**全部命中**；`static_check.js` 的 147 / 97 / 163 / 766 四个子数字；
两套套件逐套条数；`localStorage` 11 键；`ST_TABS`(14) / `ST_CODE_TOOLS`(19) /
`ST_CODE_TEXT_PATHS`(15) / `ST_CODE_LIST_PATHS`(4) / `AI_PROVIDERS`(7) / `ST_AI_PROVIDERS`(13) /
`ST_AI_MOOD_PRESETS`(14) / `ST_AI_FORMAT_PRESETS`(7) / 十余个 `ST_CODE_*` 常量；
`02-ai-chat.md` 的供应商表与源码逐行一致；`01`/`05`/`06` 无行号类引用。

---

## 3. 已修：3 处「旧结论」还挂在现行文档里

这是本轮**最有价值**的一类 —— 不是数字错，是**结论**错，而且和 `RULES.md` 六 直接矛盾。
只读 `README` 的人会得出反的结论。

| 位置 | 旧结论（错） | 改成 |
|---|---|---|
| `README.md` §7 | 「`stCodeProgressClear(id);` 那一行**从来不存在**」 | 「**写进去 → 落盘 → 跑绿 → 之后被静默弄丢**」+ 依据（E11 断言的可通行性反推）+ 无当时快照的声明 |
| `README.md` §7 | `663/0` vs `661/2` 是「**套件间状态串味**」 | 「**先怀疑两次跑的不是同一份文件**」+ `_probe-falsegreen.js` 的证伪数字 |
| `CHANGELOG.md` 旧小节 | 原文**保留不改**（变更记录不该被改写） | 各加一条**指回「续三」的更正框** |

---

## 4. 抓到但当时**没改**的（后来逐项收口，见各节 ✅）

### 4.1 `blk0.js` 有两份，**两份都过时** ⚠ —— ✅ **已办：两份都删了**

| 文件 | 状态 |
|---|---|
| `G:\saki\_verify\blk0.js` | 缺整个 `progress` 能力（`ST_CODE_SANDBOX_PERM` 还是 `['card.read','card.write']`）。**已重新 extract** |
| `G:\saki\blk0.js`（仓库根目录） | 更旧（09-19 19:19）。**没有任何脚本读它**，纯遗留 |

- 好消息：`check.js` 早就不读 `blk0.js` 了（只读 `saki.html`），所以过时**没有**污染任何检查结果。
- 坏消息：一个「看起来像源码」的生成物摆在根目录，**随时会被当成最新的那份**。
- **建议**：根目录那份删掉（属破坏性操作，等你点头）。

> ✅ **已办（2026-09-20 续五 / 第十轮）**：两份**都删了**（`G:\saki\blk0.js` 1 071 629 字节 +
> `_verify\blk0.js` 1 088 113 字节）。删之前先证明 `extract.js` 一条命令可再生
> （1 090 948 字节、`node --check` 通过），再把三个读者逐个改掉：
>
> - `_probe-escape.js` —— 崩，**看得见** → 改成当场读 `saki.html` 抽内联 `<script>`；
> - `_reverse5.js` 探针 A —— **静默**（它本来就自己造那个文件）→ 改成**自造自删**；
> - `run-all.js` —— 对账数字对不上 → `NOT_A_SUITE` 里的注释改成实话（「平时不该在目录里」）。
>
> ⚠ 教训：**删生成物之前先 `grep` 出所有读者，并逐个判断「文件不存在时它会怎样」** ——
> 崩掉的那种一眼就看见，**静默失败的那种不会在绿灯里露头**。

### 4.2 CDP 临时 profile 泄漏 **14.5 GB** —— ✅ **已修**（残余 943 个 / 13.13 GB 也已清，见「续八」）

本地 12 套里 **11 套**走 CDP，每套都是：

```js
const profile = fs.mkdtempSync(path.join(os.tmpdir(), 'cdp-xxx-'));
// … 当 --user-data-dir 用 …
// 收尾：没有 rmSync
```

**每跑一次全量就多 11 个目录，从不回收。** 实测：

| 前缀 | 个数 | 占用 |
|---|---|---|
| `cdp-*` | **933** | **约 14.5 GB** |

**根因不止一个 —— 第二个是「静默失败」**：

- 11 套**完全没写** `rmSync`；
- `st-ai.js` **写了**，但没有 `maxRetries` —— Windows 上进程刚 kill 掉时目录还锁着，
  裸 `rmSync` 抛 EBUSY，被外层 `catch` 吞掉。**语法对、断言绿、每次都失败。**

**改法**（25 处 `mkdtempSync` 全覆盖）：

| 位置 | 改法 |
|---|---|
| 11 套套件 + `st-ai.js` | 收尾补 `fs.rmSync(profile, { recursive, force, maxRetries: 8, retryDelay: 150 })` |
| `sb-verify5/6/7.js` | 放在 `await sleep(200)` **之后**（等进程退干净再删） |
| `sb-verify3.js` | 抽成 `cleanupProfile()`，成功 / 异常两条路都调 |
| `smoke.js` + 4 个一次性探针 | ⚠ `prof` 在 IIFE 里、外层 `.catch` **够不着** → 改成**按前缀扫** |
| 外部 6 套 | ⚠ **本行当时写错了**（见「续七」）：实为 **7 个文件**、而且**每一个都是裸 `rmSync`** —— ✅ 已补 `maxRetries` |

**验证（量副作用，不是看代码）**：连跑两次全量 —— 退出码 0、**1664 / 0**、`cdp-*` **净增 0**。

> ⚠ 教训：**「收尾清理」这类代码，光看有没有调用不算验证。**
> `rmSync` 写在那儿、语法正确、断言全绿，而它**每次都在静默失败**。
> 判据只能是「跑之前数一次、跑之后再数一次」——
> 第一次修完净增是 **1** 而不是 0，正是这个「再数一次」才把 `st-ai.js` 揪出来。

- ⚠ **已堆出来的那些目录我没动** —— 在系统临时目录下、不属于仓库，删除属破坏性操作，**等你点头**。
  2026-09-20 续七实测：**943 个目录 / 329 163 个文件 / 13.13 GB**
  （`cdp-*` 933 + `diag-` 4 / `font-` 3 / `diagth-` 1 / `fontd-` 1 / `fontpx-` 1），
  最旧 **2026-09-17 12:22**、最新 **2026-09-20 07:13**。
  ⚠ 顺带：`cdp-tavern-` 那 39 个最新一个是 **09-19 22:26**，而今天跑过 4 次外部全量都没新增 ⇒
  **那是历史遗留，不是现在的泄漏率**。
- ✅ **已清（2026-09-20 续八）**：用户点头后全清 —— `_verify/_purge-tmp.js --go` 报
  「先数 943 → 删除成功 **943 / 943** → 删完再数 **0**」。清之前先跑了一次本地全量确认
  `cdp-` 933 → 933（净增 0），所以清的是**积压**不是**泄漏率**。

### 4.3 三处**死声明**（零引用）

| 标识符 | 行 | 说明 |
|---|---|---|
| `buildRequestMessages()` | 12956 | 一行包装 `buildRequestPlan().messages`，注释写「**兼容旧调用点**」，而**一个调用点都没有** |
| `PM_MULTI_SLOTS` | 7128 | 多消息槽表。真正的处理在 `buildRequestPlan` 里按 `slot.identifier === 'dialogueExamples'` 硬判，**没走这个常量** |
| `WI_SCAN_STATE_NAME` | 7194 | 扫描状态的中文名表。`WI_SCAN_STATE`（数值表）在用，这张没人用 |

全文件 + 两套 harness 都确认零引用。

**为什么没删**：① 删 3 处声明会让**后面所有行号整体前移** —— 刚修好的 README §2/§3
和 `RULES.md` 的行号引用会再漂一轮；② 「清理死代码」和「修 bug」是两件事，不该顺手做。
**建议**：下次改 Code 页时一起清掉，清完统一重取行号。

> ✅ **已办（2026-09-20 续六）**：三处都删了。实测漂移量是 **8 行**、受影响的文档锚点 **9 处**
> （README 6 处 + `04-card-editor.md` 头部 3 行共 5 个数字），一次修干净 —— 比预想的便宜。
> 位移是**分段**的：`< 7127` 不动 · `7127~7193` −2 · `7194~12954` −3 · `≥ 12955` −8。
>
> ⚠ 顺带抓到两处**不是这次造成的**问题：
> - README 的规模行**有两处**（顶部 + §1），只改一处会留下一个假数字。
> - `04-card-editor.md` 头部那 5 个数字**在我动手之前就已经漂了 259 / 264 行** ——
>   上一轮审查扫了 README 的 §2/§3/§6/§7，**没扫这份文档的头部**。
>
> ⚠ 通则：**「审查过了」是按文件数算的，不是按结论算的** —— 没扫到的地方不会自己报出来。
>
> 另：`PM_MULTI_SLOTS` 专门查过「是不是该把两个分支改成遍历它」—— 两个分支**不一样**
> （`kind: 'examples'` vs `kind: 'history'` + `sawHistory` 标志），合并反而是重构。
> 常量想说的那句「这两个是多消息槽」，代码里 12858 行的注释已经写着。

### 4.4 `stCodeMemoSet` 静默截断 —— ✅ **已办**

`19996`：`.slice(0, ST_CODE_MEMO_CHARS)`。

- 用户在工具条「📝 记忆」里**手输**超过 4000 字 → 保存值被截断，**界面上不提示**，刷新后尾巴没了。
- 对比：模型走 `remember` 工具那条路是**拒绝**并回一句人话（18099~18103），**符合文档**。
- textarea 没有 `maxlength`，`📝 记忆 (N)` 里的 N 是**行数**不是字数，所以用户看不出来。
- **建议**：加 `maxlength` + 在标题上补「N 字 / 4000」。

> ✅ **已办（2026-09-20 第十轮）**：两道闸 + 一处可见 —— textarea 加 `maxlength`；
> `stCodeMemoSet` 保留 `.slice` 作兜底；计数器 `stCodeMemoCounter()` 到顶变 `st-note st-bad`
> 并写明「再写不会保存」；折叠摘要改成 `📝 记忆 (N 条 · C/4000 字)`（渲染与更新共用
> `stCodeMemoLabel()`，两处不会再漂）。
>
> 补了 **8 条断言**（含「没到上限时 `className` 必须是 `st-note`」的**对照组**），
> `st-code.js` 663 → **670**。
> ⚠ 顺带：原断言 `/📝 记忆 \(2\)/` 是因为**标签格式被改对了**才失效的 ——
> **断言会随产品一起漂**，所以新断言全部现算、不写死条数与字数。

---

## 5. 审查方法（下次可以照用）

1. **零引用声明扫描**：顶层 `function` / `const|let|var` 在**整份文件**里出现 ≤ 1 次。
   ⚠ 我第一次只扫 `<script>` 块，扫出 28 个「零引用函数」—— **27 个是假阳性**，
   它们是内联 handler 的目标（`onclick="toggleCalendar()"`），引用在 **HTML 属性**里。
   **扫引用必须扫整份文件。**
2. **「注释承诺了调用但附近没有」扫描**：注释行含 `收尾/记得/必须` + 点名的 `stXxx`，
   看后 6 行有没有真调用 → 10 条候选，逐条人工看**全部是交叉引用**。
   本轮**没有**再抓到「注释说了什么 ≠ 代码做了什么」那种真 bug（上轮那个已经修了）。
3. **枚举式核对**：凡是文档写「N 个 / 共 N 条 / 第 N 行」，一律**从源码现数**，不凭印象。
4. **镜像核对**：`markdown/` 下 4 份镜像 + `SKILL.md` 全部 `cmp` 逐字节一致。
5. **改完逐条 grep 复核**：23 条改动全部 grep 过一遍（批量 Edit 静默漏是老毛病）。

---

## 6. 本轮踩到的操作坑（已记进日志）

写 CHANGELOG「续四」那节时，`old_string` 用了 `---\n\n## 2026-09-20（续三）· …`，
`new_string` 以 `---` 结尾却**没把那个 `## ` 标题带回去** ⇒ 续三的**标题被吃掉**、
正文吊在续四下面。`grep '^## '` 当场抓到，已补回。

> ⚠ 通则：**用「标题行」当锚点做插入时，要么把它包含在 `new_string` 里，要么换锚点。**
> `old_string` 含 `## ` 而 `new_string` 不含 = 删标题，而且**不会有任何报错**。
> 这是同一个坑的**第二次**（第一次在「续二」那次）。

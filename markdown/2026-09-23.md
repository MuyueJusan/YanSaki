# 2026-09-23 · 编写器新增「🎭 人设生成器」（第 11 轮）

> 上一轮（第 10 轮，内联小游戏「复古线框战机」）的收尾记在 `2026-09-21.md` 的「第三段」。
> 这一轮只做一件事：给角色卡编写器加第 15 个选项卡。

---

## 一、需求（用户原话）

> 请在角色卡编写器中增加一个项目，【人设生成器】，用户输入设定或者要求，ai根据输入内容，生成人设，
> 生成的人设依据模板，用户可以自行设定，也可以使用默认模板，默认模板如下：
> 基本信息: / 姓名: / … / 补充：

拆开是三件事：**① 用户写一句设定 → ② AI 按模板逐项填 → ③ 模板可自定义、也可用默认**。

## 二、插在哪儿、叫什么

`ST_TABS` 里插在 `desc`（角色描述）**之后**。理由：它是「写角色描述」的邻居，产出的东西也正是
`card.description` —— 放在第 4 位，用户从「角色描述」往右一眼就看见。

插完之后：**14 → 15 个选项卡**，`code` 从第 13 个变第 14 个（外部 harness 的 P69 和视觉层
就吃在这上面，见 §七）。

## 三、三个设计决定（写进代码注释了）

1. **模板存浏览器本地**（`localStorage` 键 `stPersonaTpl`），**不写进卡**。
   同 AI 配置那条理由：它是**用户自己的偏好**，不是角色卡的一部分。
2. **生成结果不自动进卡。** 先落进 `stEditor.personaOut`，面板上给你看、**可以直接手改**，
   点「写入」才进角色描述 —— 「生成的东西必须先能反悔」，跟第 9 轮「AI 生成预制块」同一条规矩。
3. **覆盖非空角色描述前弹 `confirm`。** 这是这一族里**唯一不可逆**的动作，不能静默做。
   追加那条路不用问（它不破坏已有内容）。

## 四、写套件时逼出来的那个真 bug（本轮最有价值的一条）

面板上写着「**清空 = 回到默认模板**」，而当时的实现是：

| 位置 | 行为 | 判定 |
|---|---|---|
| `stPersonaTplSet('')` | 内存设成 `''` + `localStorage` **删键** | ✅ 这半边是对的 |
| `stPersonaTpl()` | `typeof stEditor.personaTpl === 'string'` 成立 ⇒ **把空串原样返回** | ❌ |

于是清空之后，面板上**同时出现三种互相矛盾的说法**：

- 标签写「模板（默认）」—— 因为它问的是 `localStorage`（键已删 ⇒ 默认）
- 框里是**空的** —— 因为 `stPanePersona` 拿 `stPersonaTpl()` 的值渲染
- 点「生成人设」报「**模板是空的**」—— 因为 `stPersonaRun` 拿到的也是空串

修法：`stPersonaTpl()` 在内存值是空串时**也回落默认**：

```js
return stEditor.personaTpl.trim() ? stEditor.personaTpl : ST_PERSONA_TPL_DEFAULT;
```

⚠ **这条是写断言时逼出来的。** 要断言「清空 = 回到默认」，就得先把这句话的**准确定义**想清楚 ——
「回到默认」到底是「存储回到默认」还是「显示回到默认」还是「生成时用默认」？
三个都写清楚之后才发现原实现只做到了第一个。

⇒ 这正是 `RULES.md` 六里那句「**断言的方向本身就是需求**」的又一例。
`_reverse13.js` 的 **R1** 就是把这个 bug 按回去，盯着这两条断言。

## 五、模板识别的两个坑

### 5.1 冒号半角 / 全角混着用

用户给的那份模板**本来就是混的**：`姓名:` 半角、`三围：` 全角、`- 风格：` 全角带前缀。
识别必须是 `/^[\s\-]*([^:：\n]{1,12})[:：]\s*$/`。

只认半角会**静默少认 9 项**（三围 / 气味 / 校园·工作的日常装 / 风格 / 标志性穿着 / 配饰习惯 /
泳装 / 内衣 / 补充），而**面板照样渲染、生成照样成功、提示照样打 ✅** —— 只有
「项数**独立算一遍**」抓得住。

⚠ 套件里那个「独立算一遍」是**换一种写法**重算的（剥掉行首 `- `、看行尾是不是冒号），
**没有抄产品那个正则** —— 抄正则的话，正则写错时两边一起错。
`_reverse13.js` 的 **R4** 就是这一针。

### 5.2 字段要按名字去重

`风格` / `标志性穿着` / `配饰习惯` 在**三种装束**下各出现一次 ⇒ **42 行 → 36 项**。
数出来的是「**字段数**」，不是行数。面板上写「N 项」，用户拿它对的是「我写了几个格子」。

## 六、围栏清洗只做一种

模型偶尔把整份东西包进 ` ``` ` 围栏。**只洗这一种**：

```js
const fence = s.match(/^```[a-zA-Z]*\n([\s\S]*?)\n?```$/);
if (fence) s = fence[1].trim();
```

⚠ 再多的「智能」清洗（比如顺手删掉开头那句「好的，这是人设：」）会**吃掉真正的第一行**，
而那种错**看不出来**：输出照样有内容、照样能写进卡、用户照样会点写入。
⇒ 套件里必须配「**没包围栏的不许动**」作对照（`_reverse13.js` R3 的 green 名单里就有它）。

## 七、加一个选项卡，外部 harness 收到两笔账

**两笔都是「写死的清单 / 数字」，不是产品坏了** —— 但都得改：

| 位置 | 症状 |
|---|---|
| `verify_steditor.js` **P69** | 一张写死的 `ST_TABS` id 数组，缺 `persona` ⇒ **1 项红** |
| `verify_tavern_visual.js` | **5 处**写死「14 个选项卡 / 14 个标签 / 14 个图标」+ 两张写死的文案 / id 清单 ⇒ **5 项红** |

⚠ 视觉层那份 `tabWalk` 的 `[id, marker]` 清单旁边本来就写着
「加选项卡忘了改这里，覆盖就会**静默少走一页**」—— 这次它**没静默**：
清单少了 `persona`，上面那条「N 个选项卡都能切过去」当场就红了。**清单和计数要一起改。**

⇒ 这两笔正是 `MEMORY.md` ⑧ 里「**写死清单 / 写死数字的断言每次加东西都会来收一次账**」的实证。

## 八、新套件 `persona-verify.js`（第十六个套件，154 条）

十一 段：A 装载 / B 选项卡 / C 面板元素 / D 模板逻辑 / E 状态而不是 DOM / F 提示词 /
G 未配 AI / H 生成成功 / I 写入与追加 / J 复制 + 忙态 + 失败路径 / K 收尾零报错。

几个值得记的点：

- **B 段走真按钮**（`document.getElementById('st-tab-persona').click()`）而不是直接调
  `stSwitchTab` —— 要验的正是「**按钮接对了没有**」。
- **C 段配了反向对照**：还没生成时，结果框与「写入 / 追加 / 复制」三个按钮**不该在**。
  少了这几条，「结果框一直都在」这种退化会被前面几条掩盖。
- **E 段走真实输入路径**：改 DOM 的 `value` 再 `dispatchEvent(new Event('input'))`，
  而不是直接调 `stPersonaReqSet` —— 后者只证明「那个函数写得对」，
  证明不了「输入框接上了它」。
- **H 段的两条对照**：漏项 ⇒ `warn` 且说出漏了几项；全中 ⇒ `ok`。
  只说「成功」的话，**模型悄悄改写了标题用户是看不见的**。
- **J 段的忙态测试**：给 1.5 s 的假延迟 → 点真按钮 → 中途 `stRerender()` 一次 →
  断言按钮**还是**「⏳ 生成中…」。忙态只活在 DOM 里的话这里就穿帮了。

### 8.1 第一版红的 7 条，全是**测试自己写错**

| 红的是 | 真因 |
|---|---|
| 「项数跟独立算出来的一致」 | **数组用 `===` 比** —— 引用相等，内容一样也永远不等 |
| 「全角冒号那几行也认」 | 同上（`[] === []` 是 false） |
| 「空描述 ⇒ 不弹 confirm」等 3 条 | 同上 |
| 「刷新之后 `stEditor.personaTpl` 是 null」 | 时机不对：渲染面板时就会调 `stPersonaTpl()`，等我们去读早被填好了 ⇒ 改成**手动清成 `null` 再读**，那条才真的在证「懒加载」 |
| 「状态行也标了 warn」 | 状态行是**建面板时**算出来的，改了配置**得重绘**才看得见 |

⇒ 教训写进 `RULES.md` 六：**数组别用 `===` 比**；**「懒加载」这类断言要手动把状态清回去再读**，
不能拿「刷新后的值」当证据（那中间隔了一次渲染）。

### 8.2 面板容器 id 是 `#st-panes` 不是 `#st-pane`

第一版选择器全写错了（5 处）。`#st-panes` 是外层 `<main>`，`.st-pane` 是里面那一层
（每个选项卡渲染一个）。⇒ 写新套件时**先 `grep` 一次容器 id**，别按名字猜。

## 九、反向测试 `_reverse13.js`（第八轮，5 探针）

| 探针 | 打的是什么 | 预期红 | 对照组 |
|---|---|---|---|
| **R1** | `stPersonaTpl()` 不再「空则回落默认」 | 2 | 11 |
| **R2** | `stPersonaApply` 去掉覆盖前的 `confirm` | 5 | 13 |
| **R3** | `stPersonaText` 去掉围栏清洗 | 3 | 12 |
| **R4** | `stPersonaLabels` 只认半角冒号 | 5 | 7 |
| **R5** | `stPersonaTplSet` 清空时存空串而不是删键 | 1 | 5 |

**结果：16 条预期红全中、48 条对照全绿、无预期之外的红**，收尾产品与基线逐字节一致。

⚠ **存在性闸门当场抓到一条漂掉的名字**：`red`/`green` 里手抄的
「空描述 ⇒ 提示说已写入」，实际断言名是「**提示说已写入**」（没有那个前缀）。
闸门在**跑整跑之前**就拦下并退出 —— 省掉 5 次整跑（约 80 s），
而且避免了一种更坏的结局：`green` 里放不存在的名字会让「对照组一条都没红」**天然成立**。

⚠ **R5 只该红 1 条**：因为显示那一层（`stPersonaTplIsDefault` 的 `v.trim()`）本来就容得下空串，
所以这一针的粒度**落在存储上**，不落在显示上。多红了说明套件里有别的东西顺带盯着存储 ——
那是意外耦合，值得看一眼。

## 十、验证结果

| 项 | 结果 |
|---|---|
| `check.js` 静态 5 项 | **5 通过 / 0 失败** |
| 本地 16 套 | **2 211 通过 / 0 失败**（新增 `persona-verify.js` 154） |
| 外部 8 层 | **1 631 通过 / 0 失败** |
| 反向测试 | **全部达标**（5 探针 / 16 红 / 48 绿 / 无意外红） |
| 行尾 | **CRLF 29 106 / 孤立 LF 0** |
| 产品 | 1 467 029 → **1 487 410 字节**（+20 381）、28 741 → **29 107 行**（+366） |

⚠ 两套 harness **串行**跑（本地 3 m 48 s，外部 3 m 15 s），没有并行 ——
`MEMORY.md` ⑧ 那条「并行会让某套只跑一部分而 runner 照样打 ✅」的坑。

## 十一、一个自己踩的新坑（Edit）

想「在 `README` 表格的 `st-ai.js` 行**后面插一行**」，`old_string` 却写成了**那一行本身** ⇒
`st-ai.js` 整行被**替换掉**（不是被删，是被新行顶掉了）。

同一个错在 `CHANGELOG` 又犯一次：`old_string` = `---\n\n## 2026-09-21（第三段）…`，
`new_string` 结尾只写了 `---` ⇒ **下一段的标题被吞**。

⇒ 写进 `MEMORY.md` ①：**「插入」要把锚点行也抄进 `new_string`**；
改完**立刻回读那一段**（这次两条都是靠回读发现的，不是靠记忆）。

## 十二、收尾清单（这一轮的完整动作）

1. 产品三处插入（`ST_TABS` 条目 / `stEditor` 状态 / `stPaneHtml` 分派）+ 一整块新代码
2. 修 `stPersonaTpl()` 的空串回落（§四）
3. 新套件 `_verify/persona-verify.js` + 登记进 `run-all.js`（15 → 16 套）+ 单套 timeout 4 min → 15 min
4. 改 4 处写死的「14」：`smoke.js` / `st-ai.js` / `st-code.js` / 外部 `verify_steditor.js`
5. 外部 `verify_steditor.js` 的 C2 / C3 两张对照表插入 `人设生成器` / `st-tab-persona`；
   `verify_tavern_visual.js` 补 `persona` 到 `tabWalk` 清单 + 5 处「14」→「15」
6. 反向测试 `_reverse13.js`
7. 文档：`04-card-editor.md`（选项卡表 14→15、新增 §十六、后面四节顺延编号、
   「第 6 个选项卡」→「第 7 个」）、`README.md`（套数 / 条数 / 各处的「15 套」→「16 套」、
   §7 表新增一行、`smoke.js` 89→90）、`CHANGELOG.md` 新增一段、`RULES.md` 新增「一之补三」
8. 记忆：`MEMORY.md` 合并去重（18 087 → **16 307** 字节，净减 1 780，同时还加了新内容）
9. 四份镜像 `cp` + `cmp` 逐字节核对
10. `_audit-anchors.js` / `_audit-counts.js` 复核

## 十三、收尾时又抓到一个「顺手 +1」的错

README §7 那段讲 CDP 临时 profile 泄漏的，原文是：

> ⚠⚠ **每一套 CDP 套件都会在系统临时目录里留一个 Chrome profile，而且不删。** …
> 本地 **15 套里 14 套**走 CDP ⇒ **每跑一次全量就多 14 个目录**。

加完第 16 套之后，我顺手把「15 套里 14 套」改成了「16 套里 15 套」—— **这是错的**。

量了一下才发现：**16 套里走 CDP 的那 15 套，`rmSync` 一处不缺**（`mkdtempSync` 16 处、
`rmSync` 15 处），一段里本来就自带「✅ 已修（2026-09-20）… 连跑多次全量，`cdp-*` 净增 0」。
也就是说那一整段记的是**当年的现场**，我改的却是**现在的数字** ⇒ 上下自相矛盾。

**实测证据**：今天跑完 16 套（3 m 48 s）+ 反向测试 6 次，`_purge-tmp.js` 报的 `cdp-*`
仍然是 **2026-09-21 那 7 个旧目录**，**新增 0**。

改法：把那一段**明确标成「历史状态（2026-09-20 审查时）」**，数字还原成当时的 15/14，
并补一条今天的复量（净增 0）。

⇒ 教训：**「改一处数字」之前先问「这个数字记的是哪个时刻」**。
套数变了不等于它描述的**现象**也变了 —— 顺手 +1 会把一条**已经作废的结论**一起搬到现在。
（同族：`MEMORY.md` ① 里那条「同一个数常有多种载体 —— 改一处要 `grep` 全部」。）

---

# 第二段 · 把「🎭 人设生成器」挪到 Code 后面

> 用户原话：**「将人设生成器的选项卡挪到【code】的下面」**。
> 一句话需求，但它是**纯位置调整** —— 而「挪位置」和「加项目」要改的东西**不是同一批**。

## 十四、数量不变，所以「跟数量绑定」的断言一条都不用改

先分清两类：

- **跟数量绑定**：`ST_TABS.length === 15`、`#st-tabs .st-tab` 数 15、折叠后 15 个标签、
  `smoke.js` 的「15 个选项卡逐个切」、`st-ai.js` 的「选项卡数量变成 15」…… **全都不用动**。
- **跟顺序绑定**：会来收账。产品里 `ST_TABS` 一处；套件里 4 处。

## 十五、顺序型断言有三种长相，两种**根本不会红**（本轮最有价值的一条）

| 长相 | 在哪 | 会不会红 |
|---|---|---|
| ① 遍历清单是**字面量数组** | `verify_tavern_visual.js` 的 `[[id, marker], …]` | ❌ **不会红** —— 新项不在里面，只是静默少走一页 |
| ② **子串正则** | `st-code.js` 的 `/sb,code,export/` | ❌ **不会红** |
| ③ **相邻距离写死** | `persona-verify.js` 的 `pi - di === 1` | ✅ 会红 |

② 是这一轮抓到的：`ST_TABS.map(t => t.id).join(',')` 变成
`…,sb,code,persona,export,…` 之后，`/sb,code,export/` **照样匹配** ——
子串 `sb,code` 还在，正则跑到 `export` 就不再看后面。**它没红，只是把 `persona` 从覆盖里静默漏掉。**

⇒ 改法：把整段列全（`/sb,code,persona,export/`），并把断言名改成
「code 排在 sb 和 export 之间、**persona 紧跟其后**」—— 名字里写明现在盯的是什么，
下次再挪**光看名字就知道该改哪条**。
⇒ 教训：**顺序型断言别写成「子串在不在」**。要么列全，要么用相邻距离。

## 十六、③ 会红，但光改数字不够 —— 要补对照组

`pi - di === 1` → `pi - ci === 1`（`ci` = `code` 的下标）。**再加一条反向对照**：
「它**不再**跟在「角色描述」后面」（`pi - di !== 1`）。

理由：正断言只要求「挨着 `code`」，**不要求「离开 `desc`」** —— 位置其实没挪动也能让它绿。

⚠ 这条对照**第一版我自己写错了**，套件当场抓住：`check(name, actual, pred, expect)` **只收 4 个参数**，
我按正断言的形状写成 5 个（`pi - di, 1, v => v !== 1, '≠1'`）⇒ `pred` 成了字面量 `1`，判 `11 === 1` → 红。
**函数谓词只占一个位置，后面只剩一个「给人看的期望值」。**
（这是套件在**帮我**，不是在坏 —— 那条对照组确实值得存在。）

## 十七、产品里三处过时注释

| 行 | 原文 | 改成 |
|---|---|---|
| `2385` | 全屏浮层里左侧是**十四个**选项卡 | 十五个 |
| `15269` | 左侧**九个**选项卡（可以折叠成一条图标栏） | 十五个 |
| `5525` | **用户可见文案**：「左侧**九个**选项卡分别管 名字/…/导出」 | 十五个，且把 15 项**列全** |

⚠ 第 3 处是**用户能看见的**，而且原文连 `ai` / `th` / `mvu` / `sb` / `code` 都没写。
⚠ 改前先确认**两套 harness 里没有任何断言读那段文案** ⇒ 安全可改。
⚠ **产品注释里的数字对所有静态检查隐身**（`check.js` 五项一个都不看注释），只能靠 `grep`。

## 十八、顺手修掉两处早就漂了的数字

- `04-card-editor.md` 说 `st-code.js` **663 条**，实际 **670** —— 上一轮改了 `README` §7 却没同步分册。
- 同分册里 `verify_tavern_visual.js` 那段「**十四个**页签切换」→ 十五个。

⚠ 判据：663 是「当时没同步」而不是「历史状态」，所以该改；
而 `CHANGELOG` 里那堆 663 是**历史记录**，一个字都不动。
（同族：第一段「十三、顺手 +1 的错」。**同一个数字，两种时刻，两种处置。**）

## 十九、`MEMORY.md` 触发了注入上限，被截断 —— 做了一次真合并

系统提示 `MEMORY.md` **超限、注入时被截断**（快照正好切在第 ④ 条中间）。

做法：它本来就是「**只是索引**」，但 ②–⑦ 那节把 `RULES.md` 的细节抄了一遍 ——
**抄一份就一定会分叉**。于是把 ②–⑦ 改成**一张表**（主题 / 展开版在哪 / 最要命的那一条），
把 ⑧ 的四条并成三条、`check.js` 与「跑套件」各压一压。

结果：**8 754 字符 / 16 471 字节 → 7 067 字符 / 13 308 字节**（≈ -19%），同时**加进了本轮的新教训**。

⚠ 顺带发现：**镜像的方向**。`markdown/RULES.md` 是 `.workbuddy-ai/memory/RULES.md` 的**副本**，
我一开始改的是副本 ⇒ `cmp` 立刻报不一致。**改源、再 `cp`**。

## 二十、验证结果（第二段）

| 项目 | 结果 |
|---|---|
| 本地 16 套（`run-all.js`，4 m 01 s） | **2 212 通过 / 0 失败**（`persona-verify.js` 154 → **155**） |
| 外部 8 层（`run-all.js`，3 m 17 s） | **1 631 通过 / 0 失败**（`verify_steditor.js` 520/520、`verify_tavern_visual.js` 371/371） |
| 反向测试 `_reverse13.js` | **全部达标**：基线 155 绿、5 个探针 18 条名字**一条不漂**、预期红全中、对照全绿、无预期之外的红、产品哈希逐字节还原 |
| `check.js` | 5 通过 / 0 失败 |
| 产品 | 1 487 410 → **1 487 774 字节** / 29 107 → **29 110 行** / CRLF 29 109 / 孤立 LF **0** |

⚠ 反向测试必须重跑的理由（`RULES.md` 六之十一）：**改了靶子套件的断言名** ——
基线条数从 154 变 155，探针的 `red`/`green` 名单如果漂了就全白跑。这次核对过：**18 条名字一条不漂**。

## 二十一、收尾审计又抓到两笔 —— 都是「挪位置」特有的

跑 `_audit-anchors.js` / `_audit-counts.js` 之后才发现的，**套件全绿也看不见**：

### ① 序数漂了：「AI 助手是第 7 个选项卡」

`persona` 从第 4 挪到第 14，**它后面所有选项卡的序号整体 -1**：

| 选项卡 | 挪之前 | 挪之后 |
|---|---|---|
| `sys` | 5 | 4 |
| `meta` | 6 | 5 |
| **`ai`** | **7** | **6** |
| `book` | 8 | 7 |
| … | … | … |
| `sb` | 13 | 12 |
| `code` | 14 | 13 |

文档里有三处写着序数，`grep -rn "第 [0-9]* 个选项卡"` 才找得到：
`RULES.md` 的节标题「二、AI 助手（第 7 个选项卡）」、`04-card-editor.md` 正文
「配置页是第 7 个选项卡」、`MEMORY.md` 表格里的「③ AI 助手（第 7 个选项卡）」。

⚠ **静态检查一个字都看不见**（注释 / 分册 / README 三种载体，`check.js` 五项全不看）。
⚠ **别手算** —— 手算出来的正是会漂的东西。从产品实测：
`awk '/const ST_TABS = \[/,/^        \];/' saki.html | grep -o "id: '[a-z]*'" | cat -n`。

### ② 我那段用户可见文案多了 1 行 ⇒ README 锚点表整体 +1

`5525` 那段文案从 4 行变 5 行，于是**它后面**的锚点全 +1：

| 锚点 | 表里写 | 实际 |
|---|---|---|
| `#st-overlay` / `#st-shell` | 5541 / 5542 | **5542 / 5543** |
| `.game-entry-btn` | 5566 | **5567** |
| `.hug-button` | 5571 | **5572** |
| `#gameModal` | 5576 | **5577** |
| `#gameCard` / `#gameShooterWrap` / `#gameShooterFrameHost` | 5577 / 5640 / 5641 | **5578 / 5641 / 5642** |

⚠ `_audit-anchors.js` **只扫表内那 9 个**，表外那三个得自己 `grep`。
⚠ **「加了 3 行」≠「锚点都 +3」** —— 改动分散在两处（`5525` +1 行、`ST_TABS` +2 行），
只有各段**之后**的锚点才动。⇒ **别按加法推，一律重新 `grep`**（`MEMORY.md` ① 那条的又一次应验）。

⇒ 这两笔合起来说明一件事：**「挪位置」的收尾清单比「加项目」长** ——
加项目要查「数量」，挪位置还要查「序数」和「锚点位移」。

## 二十二、修掉 `04-card-editor.md` 一个**老**问题：两节都叫「二十」

顺手扫了一遍全部文档的节号：

```bash
cd markdown && for f in *.md; do
  dup=$(grep "^## " "$f" | sed 's/^## \([^、]*\)、.*/\1/' | sort | uniq -d | tr '\n' ' ')
  [ -n "$dup" ] && echo "$f → 重复: $dup"
done
```

**只有一处**：`04-card-editor.md` 的 `## 二十、历史上修过的真 bug` 和 `## 二十、测试` 撞号。

修法：把**最后那一节**改成 `## 二十一、测试`（改尾部只影响引用它的人，不动前一节的号）。
连带两处交叉引用 `§二十 新增 sb-verify8.js 十段表` → `§二十一`（`2026-09-21.md` 与 `CHANGELOG.md`）。

⇒ 判据：**交叉引用是「指针」，不是历史事实。** 指针要指对地方，所以该改；
而「当时是 12 个」这类**数字**是历史事实，一个字都不动（同第一段「十三」那条）。
⇒ 在 `## 二十一、测试` 底下留了一段说明 + 「别改回去」，省得下次有人当成手误。

⚠ 这类撞号**没有任何工具在看**（`check.js` 只扫产品；`_audit-counts.js` 扫的是「N 个 X」；
`_audit-anchors.js` 扫的是行号）。上面那段 `for` 循环目前是唯一的手工闸门 ——
**要不要把它固化成 `_audit-sections.js`（并登记进 `run-all.js` 的 `NOT_A_SUITE`）**，留给下一轮定。

## 二十三、给项目接上 GitHub（私有仓库 + PAT）

用户要求：**整个 `G:\saki`（除 `.workbuddy-ai/`）、私有仓库、PAT 认证、以后我改完顺手推**。

### 环境实况（探过，不是猜的）

| 项 | 状态 |
|---|---|
| git | **2.55.0** ✅（PortableGit，另有系统 Git 在 `C:\Program Files\Git`） |
| `gh` | **未安装**（`winget` 可用，需要时可 `winget install GitHub.cli`） |
| Git Credential Manager | **2.9.0** ✅（`git-credential-manager.exe` 在 PortableGit 里） |
| `G:\saki` | 原**不是** git 仓库 |
| 全局身份 / 凭据 / `~/.ssh` | **全空** —— 从零开始 |

### ⚠⚠ 本轮最重要的发现：系统级 `core.autocrlf = true`

`git config --show-origin --get core.autocrlf` → `file:…PortableGit/…/etc/gitconfig` = **true**。

**这对本项目是致命的**，因为文件是**混行尾**的（实测）：

| 文件 | 行尾 |
|---|---|
| `saki.html` / `index.html` / `index2.html` / `saki - 副本*.html` | **纯 CRLF** |
| `markdown/*.md`、`_verify/*.js` | **纯 LF** |

`autocrlf=true` 下，**纯 LF 那批检出时会被悄悄改成 CRLF** ⇒
四份镜像的 `cmp` 逐字节核对、`wc -c`、sha1 **全部对不上**，而且症状离现场很远
（文件看起来「就是那些文件」）。CRLF 那批虽然往返看似无损，但仓库里存的不是磁盘上那份字节。

**两道一起上**（缺一不可）：

1. `.gitattributes` → `* -text`（「对行尾转换而言当二进制」，git 一个字节都不动）
2. `git config --local core.autocrlf false`

**验证方式**（这就是「已修」的证据，不是「我设了」）：

```bash
git ls-files --eol saki.html markdown/README.md _verify/run-all.js
# i/crlf w/crlf attr/-text   saki.html        ← 保持 CRLF
# i/lf   w/lf   attr/-text   markdown/README.md  ← 保持 LF
```

### 做了什么

- `git init -b main`；`core.autocrlf=false`（local）
- `.gitattributes`：`* -text`（注释里写明**为什么**，别删）
- `.gitignore`：`.workbuddy-ai/` + `*.bak` / `_reverse*.bak` / `Thumbs.db` / `desktop.ini`
- `git add -A` → **146 个文件**，`.workbuddy-ai/` 命中 **0** ✅，总计 **133 MB**
- 最大的文件：两个字体 zip（**34 MB / 19 MB**）—— 都低于 GitHub 的 100 MB 硬限，
  但**超过 50 MB 的告警线**（会出警告，不会失败）

### 安全核查（推之前必做）

在 `saki.html` / `index*.html` / `markdown/*.md` 里扫 `sk-` / `AIza` / `ghp_` / `Bearer …`：
**零命中** ✅。AI 的 key 只存在浏览器 `localStorage` 的 `aiChatConfig` 里，**不在文件里**。

### 还差什么

- 仓库地址（私有、空的）
- fine-grained PAT（`Contents: Read and write`）
- 拿到之后：`GET /user` 取 `login` + `id` ⇒ 设身份为
  `<id>+<login>@users.noreply.github.com`（**不用问用户要邮箱**），再提交 + 推 + 设 upstream

## 二十四、真的推上去了（首次推送 · 仓库已转私有）

> 上一节写到「还差什么」，这一节把它做完了。

### 环境实况（现量的）

| 项 | 值 |
|---|---|
| git | 2.55.0.windows.3（PortableGit） |
| 远端 | `github.com/MuyueJusan/YanSaki`，**原本公开**、`main` 已有 5 个 commit、5399 KB |
| 身份 | `MuyueJusan` / id `86054388`（PAT 调 `GET /user` 取到；email 私密 ⇒ 用 `<id>+<login>@users.noreply.github.com`） |
| 凭据 | `credential.helper = store --file=G:/saki/.workbuddy-ai/git/credentials`（本地 config；**那个文件在 gitignore 里，永不入库**） |
| 行尾 | `git ls-files --eol` → `saki.html` i/crlf w/crlf、`markdown/README.md` i/lf w/lf，全带 `attr/-text` ✓ |

### ⚠⚠ 远端不是空仓库，是一个在跑的线上站点

`GET /repos/.../git/trees/main?recursive=1` + `GET /pages` 一查就露：

- `CNAME` = **`yansaki.top`**（自定义域名）。
- `build_type = workflow` ⇒ **Pages 是靠 Actions 部署的**，靠的是 `.github/workflows/static.yml`。
- 远端 3 条 workflow，拿 `actions/runs` 一数：**`static.yml` 8/8 成功**、`hugo.yml` **0 成 9 败**、`jekyll-gh-pages.yml` 3 成 5 败 —— 后两条每次 push 都跑、都红、互相取消（concurrency group 都是 `pages`）。
- 远端 `index.html` = **1 467 029 字节**，正是本地 git 索引里那条「幽灵记录」`5888e194…` ⇒ 本地那个消失的 `index.html` 就是**上一次部署用的版本**。而 `index2.html` 的 blob `c72ba1d4…` **逐字节等于 `saki - 副本 (5).html`**。
- 远端 `index.html` 里**一个 `fonts/` 引用都没有**（`@font-face` 数 = 0），但仓库里躺着 7 MB 的 `fonts/fusion-pixel-12px-proportional-ja.ttf` —— 而**本地 `saki.html` 正是引用 `./fonts/fusion-pixel-12px-proportional-ja.ttf`**。⇒ 用户说的「字体保留」是硬需求，推上去正好补上这个闭环。

### 本地全新历史 ≠ 只能 force push

本地是 `git init` 出来的，跟远端没有共同历史 ⇒ 普通 push 会被拒。但**不用 force**：

```
git fetch origin main                        # 拿远端对象（FETCH_HEAD = 远端顶端）
git reset --soft FETCH_HEAD                  # HEAD 架到远端顶端，索引 / 工作区不动
git restore --source=HEAD --staged --worktree -- <远端独有的文件>
git add -A && git commit
git push origin main                         # 真快进 ✓
```

比 force 好在：**远端那 5 个 commit 的历史全留着**，而且「删了什么」在 diff 里看得见。

### 推之前的三个决定（用户答的）

1. 可见性：**改成私有** —— `PATCH /repos/...` `{"private":true}`，http 200 ✓。
2. 远端独有的 9 个文件：**全部保留**（`CNAME` + 3 条 workflow + 4 个旧字体 + `gemini-code-*.html`）⇒ 先补进本地再提交，**删除 0 个**。
3. 大件：**全进**（两个字体 zip 54 MB + 两份解压目录 55 MB + `saki.zip` 1.9 MB）。

### 结果（都从**远端侧**核过，不是「本地看着对」）

- `git ls-remote origin main` = `d4d7d9352e80f773a34129a949379dfd7c16cd79` = 本地 HEAD ✓
- 远端 **110** 个文件（= 本地索引 110）✓；远端内容合计 **131.4 MB** ✓
- **远端 `index.html` 的 blob = `b4ecc93cf3c2f9e90b1c691eeb0f2bbd7d737bec` = `saki.html` 的 blob，1 487 774 字节** ✓✓ —— 这就是用户要的「`saki.html` 作为 `index.html`」
- `CNAME` / `static.yml` / `hugo.yml` / `gemini-code-*.html` 全在 ✓
- `_verify/shots/*.png` **不在远端** ✓（`.gitignore` 生效）
- 提交作者 = `MuyueJusan <86054388+MuyueJusan@users.noreply.github.com>` ✓

### ⚠ 改私有的代价（用户知情后选的）

`GET /pages` 从 200 变成 **404** ⇒ **Pages 被 GitHub 关掉，`yansaki.top` 已失效**。连带 `static.yml` 从 8/8 成功变成 failure。⇒ 现在这 3 条 workflow 全是噪声；**要把站点恢复就得把仓库改回公开**。

### 踩到的坑

- ⚠ **`http.extraheader: Authorization: Bearer <PAT>` 对 git-over-HTTPS 不管用**（只有 REST API 认），报 `could not read Username`。改用凭据文件（`store --file=…`，放在 gitignore 掉的目录里）。
- ⚠⚠ **`git fetch origin main` 报 `* [new branch] main -> origin/main`，可 `refs/remotes/origin/main` 就是不存在** —— `for-each-ref` 一个引用都没有、`rev-parse` 报 ambiguous、`git update-ref` 手写引用**退出码 0 但文件没落盘**。而 `git reset --soft FETCH_HEAD` **成功写出了 `refs/heads/main`**。⇒ 不追根因也能干活：**拿 `FETCH_HEAD` 当锚点，别依赖 `origin/main`**。
- ⚠ **回收站里能找到「消失的文件」。** `G:\$RECYCLE.BIN\<SID>` 下 `$I*` 是元数据（偏移 8 = 大小、16 = FILETIME、24 = 名字长度、28+ = UTF-16LE 原路径），`$R*` 是内容。解出来：`index.html` / `index2.html` 是 **2026-09-23 06:58:55Z 被删进回收站**的；其余 129 条全是**我自己**的操作痕迹（`_verify/*.bak`、`shots/*.png`、`index.lock`、`gcm-diagnose.log`）⇒ **这个环境的删除会走回收站**，所以「文件没了」先去回收站翻，别急着怀疑产品。
- ⚠ **commit 之前先看「父提交是不是远端顶端」** —— 是，才是快进，才不用 force。

## 二十五、用户又要求改回公开 —— 顺手纠正了我上一条里说错的一句话

用户说「仓库改回公开吧」。`PATCH /repos/...` `{"private":false}` → 200，`visibility = public` ✓。
**但 `GET /pages` 还是 404。**

⚠⚠ **我上一条里写的「想恢复站点，把仓库改回公开就行」是错的。** 转私有时 GitHub 是**把 Pages 配置整个删掉**，不是暂停；转回公开**不会**自动重建。完整恢复要三步：

```
POST /repos/.../pages  {"build_type":"workflow"}                  -> 201
PUT  /repos/.../pages  {"cname":"yansaki.top"}                    -> 204
POST /repos/.../actions/workflows/static.yml/dispatches {"ref":"main"}  -> 204
```

- ⚠ **重建后 `cname` 是 `null`** —— 仓库根目录那个 `CNAME` 文件**不足以**填充 API 里的这个字段，得显式 `PUT` 一次。
- ⚠ 重建时没有任何新部署，而且转私有期间的 run 全是 failure ⇒ **必须手动 dispatch 一次**，否则站点是空的。
- 顺带发现：`https_enforced` 从原来的 `false` 变成 **`true`**（重建后反而更严了）。⇒ **重建后要复查这个字段，别假设跟以前一样。**

### 恢复验证（从外面核，不看 API 的成功提示）

| 路径 | 结果 |
|---|---|
| `https://yansaki.top/` | **200** |
| `.../index.html` | **1 487 774 字节，sha1 `ecb46ca8de994de8f967c5a5e6577d9ba0d779e0` = 本地 `saki.html` 逐字节相同** ✓ |
| `<title>` | `YanSaki的小屋` ✓ |
| `.../fonts/fusion-pixel-12px-proportional-ja.ttf` | **200**（`font/ttf`）✓ |
| `.../markdown/README.md`、`.../_verify/check.js` | 200 ✓ |
| `.../_verify/shots/` | **404** ✓（gitignore 生效，产物没被发布） |

⚠ **HEAD 的 `content-length` 是压缩后的大小**（首页 HEAD 报 377 634，GET 才是 1 487 774；字体 HEAD 报 1 737 401，实际 7 012 636）。⇒ **要比字节就得用 GET，别拿 HEAD 的数字当文件大小。**
⚠ 这次卡过一次 180 s 超时 —— 因为一口气 GET 了 7 MB 的字体。**先 HEAD 探活、只对要逐字节比对的那一个用 GET**。

---

## 二十六、跨设备开发：`setup-dev.sh` —— 技能**早就在仓库里了**，缺的是「装回去」那一步

用户说：「把总结的skill也推上去吧，方便跨设备开发」。

### 先说清一件事：技能**已经**在远端了

`markdown/SKILL.md`（277 111 字节）和 `markdown/SKILL-git-push.md`（9 753 字节）**都在 `d74aadf` 里** —— 「推上去」这步上一轮就做完了。真正缺的**不是「文件在仓库里」，而是「新设备上怎么把它装回运行时」**。

### 为什么「在仓库里」≠「能用」

`.workbuddy-ai/` **不进仓库**（里面还有 git 凭据），所以仓库里那几份只是**镜像**，而且路径对不上：

| 仓库里 | 运行时要的 |
|---|---|
| `markdown/SKILL.md` | `~/.workbuddy-ai/skills/verify-single-file-html-app/SKILL.md` |
| `markdown/SKILL-git-push.md` | `~/.workbuddy-ai/skills/git-push-existing-github-repo/SKILL.md` |
| `markdown/MEMORY.md` · `RULES.md` · `YYYY-MM-DD.md` | `<repo>/.workbuddy-ai/memory/…` |

**一份镜像文件 ↔ 一个技能目录** ⇒ 手动 `cp` 很容易装错地方、或者**装反方向**（把镜像当源，覆盖掉本地）。这才是「跨设备」真正卡住的地方。

### `setup-dev.sh` 的三条纪律

1. **默认 dry-run** —— 不加 `--go` 只打印计划。
2. **默认不覆盖** —— 已存在且内容不同就**跳过 + 提示**，要覆盖得显式 `--force`。**这是故意的**：记忆的真相源是**本地**，在**写这份记忆的那台机器**上跑，覆盖 = 把新内容冲掉。
3. **绝不碰 `.workbuddy-ai/git/credentials`** —— 那个文件在 gitignore 里，脚本里也一个字都没提它。

### 实测（这次是真跑的，不是「应该能行」）

| 场景 | 结果 |
|---|---|
| `bash setup-dev.sh`（干跑） | 9 份镜像全「一致，不动」，**零文件改动** ✓ |
| 假 `HOME` + `--go`（模拟新设备） | 两个技能目录**新建成功**，与镜像 `cmp` **逐字节一致** ✓ |
| 改内容 + 不加 `--force` | 打印「已存在且不同，跳过」，**文件保持原样**（`tail -1` 仍是 `local edit`）✓ |
| 加 `--force` | 覆盖，`cmp` 逐字节一致 ✓ |
| `--bogus` / `--help` | 退出码 2 + 提示；`--help` 打印用法 ✓ |

⚠⚠ **测「新建」分支必须换 `HOME`**（`DEST_SKILLS` 挂在 `$HOME` 下）—— 在真机上跑**永远只会走「一致，不动」那条路**，看起来全绿，其实**那一条分支根本没被执行过**。假 `HOME` 目录用完已删。

### 顺手把 `MEMORY.md` 又压了一轮：13 671 → 12 248 字节

⚠ **合并换行不减字节** —— 第一次把 22 行并成 10 行，字节数几乎没动（只省了几个换行符）。**要真减必须删内容**，而按规矩**「删前确认 `RULES.md` 已有」**。

所以先 `grep` 核了一遍（`RULES.md` / 技能 各查一遍）：

| 条目 | `RULES.md` | 技能 | 结论 |
|---|---|---|---|
| `run-all` / `1/371` / `写死清单` / `列表符` | ✅ 7 / 1 / 1 / 1 | 部分（`run-all` 2，`列表符` 0） | 删掉重复部分 |
| `全屏类` / `转义闸门` / `手机菜单` / `假红` | ❌ 0 | ❌ 0 | **留着**（删了就真丢了） |
| `woff2` / `NOT FOUND` / `check.js` | ❌ 0 | ✅ 1 / 5 / 5 | 保留（技能是通用层，项目特有的仍留索引） |
| `对照组` | ❌ 0 | ❌ 0 | **留着**（全项目只有 `MEMORY.md` 有） |

⇒ 最后删的是**跟技能 `verify-single-file-html-app` 重复的通用反向测试纪律**（`red`/`green` 名单、注入器自包含那几条 —— 技能里 `red` 出现 276 次、`green` 61 次），**没有**为了数字好看去删项目特有的坑。

⚠ 这轮又撞上**注入截断**（`MEMORY.md` 整份注入、上限未知且快照滞后）—— 已同步到 `markdown/MEMORY.md`，`cmp` 一致。

### 文档同步

- `markdown/README.md`：镜像小节后新增 **`### 反向：在新设备上把技能与记忆装回去（setup-dev.sh）`**（三种用法 + 为什么不覆盖 + 不碰凭据）。
- `markdown/CHANGELOG.md`：新增「**2026-09-23（第四段）**」（最新在上）。
- `markdown/MEMORY.md`：同步瘦身后的版本；`markdown/2026-09-23.md`：本段。
- ⚠ 本段 **`saki.html` / `index.html` 一个字节没改** ⇒ **没跑套件**（也没有理由跑）。

### 收尾：两条新教训各自归位

- **技能 `git-push-existing-github-repo` 第 6 节**新增两块：
  ① **推送成功的判据是那行 `old..new  main -> main`，不是退出码** —— `git push | tail -4` 报的是 **`tail` 的**状态；我这次的 `for` 循环就被它骗了一次（push 明明成功了，循环却继续重试）。
  ② **git 传输被墙 / 抖动时用 REST API 兜底核对**（`GET /commits/main` + `/git/trees/<sha>?recursive=1`，公开仓库**不需要令牌**），再拿 `git hash-object` **逐 blob** 比。顺带用 tree 的 `mode` 证明 `setup-dev.sh` 的 `100755` 执行位真的推上去了 —— **Windows 上 `core.filemode` 会静默丢掉它**。
- **`RULES.md` 新增「六之十八」**：⚠⚠ **「新建」那条分支永远没被执行过，看起来也是全绿。** 本机跑 `--go` 出 9 行「一致，不动」很绿，但那 9 行**没有一行走过「新建」** —— 而「新建」才是换设备时的**主路径**。⇒ 测脚本必须**把目标目录换掉**（`HOME=/tmp/xxx`）再跑，判据是「**这条分支被执行过**」，不是「输出全是绿的」。

### 远端复核（本轮）

| 项 | 结果 |
|---|---|
| 远端 `main` | `61d09f592cb86588c8ca22466bfa8b8205f70fd9` = 本地 HEAD ✓ |
| 逐 blob（`setup-dev.sh` / 两份 SKILL / `MEMORY.md` / `index.html` / `saki.html` / `CHANGELOG.md`） | **7/7 一致** ✓ |
| `setup-dev.sh` 的 mode | **`100755`**（执行位保住了）✓ |
| `markdown/MEMORY.md` 远端大小 | **12 248 字节**（= 瘦身后的版本）✓ |
| `index.html` / `saki.html` | 同一个 blob `b4ecc93cf3`，1 487 774 字节 ✓ |
| `static.yml`（`push` · `61d09f5`） | **completed / success** ✓ |
| `https://yansaki.top/setup-dev.sh` | **200**（`application/x-sh`）✓ |
| `https://yansaki.top/index.html` | **200 · 1 487 774 字节 · sha1 `ecb46ca8de99…` = 本地 `saki.html`** ✓ |

⚠ **推的时候网络抖了**：`git push` 第一次报 `Empty reply from server`，重试第 1 次就成功（`d74aadf..61d09f5`）；之后 `ls-remote` 连着 5 次 502。⇒ **`ls-remote` 失败 ≠ 推送失败**，用 REST API 兜底才核出真相。

---

## 二十七、人设生成器：「模板」默认折叠 + 状态提示挪到模板下面（第十二轮）

> 用户原话：将【人设生成器】的工作状态提示挪到【模板】下面，顺便为【模板】设置折叠，且默认折叠起来

### 改了什么

1. **「模板」默认折叠**：折叠头 = 整行可点的按钮（`▲` + `.st-sub` 标题 + 右侧「36 项 · 点这里展开改」）。收起时占一行，摊开还是原来那份 42 行文本框。
2. **AI 状态提示（「当前生效：…」）挪到模板块下面** —— 原来它夹在模板和按钮行之间。
3. 折叠态 `stEditor.personaTplOpen`，**不落盘**（落盘的话「默认折叠」只在第一次成立）。跟 `personaReq` / `personaOut` 一样是会话级面板状态。
4. 机制沿用全站那套 `grid-template-rows: 0fr → 1fr`；⚠ **方向跟 `.ai-settings-collapse` / `.ai-adv-panel` 相反**（那几处默认展开、加 class 才收起；这里默认收起、加 `.st-fold-open` 才展开）。
5. 切换时**只改 class，不调 `stRerender()`** —— 重绘会把 pane 重建，新元素一出生就带着最终样式，过渡**没有起点**，动画会直接跳掉。
6. 折叠头里的标题**保留 `.st-sub` 类** —— 它既是「默认 / 已自定义」的可见标签，也是 `persona-verify.js` 按 `.st-sub` 找、读 `textContent` 的那个节点（换类名那些断言会红）。

### ⚠⚠ 又踩了同一个坑 —— 而那条规矩**上一轮被我误删了**

`grid-template-rows: 0fr` + `overflow: hidden` 是**裁切**，而**裁切不影响 `getBoundingClientRect()`**：里面 textarea 的 rect **照样满高**，收起 / 展开量出来一模一样。

**这条规矩上一轮还在 `MEMORY.md` 里**，我压缩索引时把它当成重复项删了 —— 而 `RULES.md` 里**本来就没有** ⇒ **两处都没了**。这一轮实现折叠时又踩一次。

⇒ **流程上的教训（比技术教训更值钱）**：「删前确认 `RULES.md` 已有」要**对着「准备删的那一条」核**，不是对着「我记得重复的那一批」核。压缩时我 `grep` 的是手上那几条，没在手边的就这么没了。

已补三处：
- `RULES.md` **六之十九**（完整来龙去脉 + 代码 + 流程教训）
- 技能 `verify-single-file-html-app` 新增一节 **「A collapsed panel's children still report a full-size getBoundingClientRect()」**（含「`inView` 要单独断言，否则点在屏幕外时『收起』天然成立」）
- `MEMORY.md` ⑧ 补一句（同时把「漏语句两形态」的展开文字收成「见六之三」，净增 99 字节）

⚠ 套件里**故意留一条**「textarea 的 rect 照样是满高」当断言 —— 看着像废话，作用是**下一个人想改回量 rect 时当场变红**，而且红在名字上就写着为什么。

### 验证

| 项 | 结果 |
|---|---|
| `node check.js` | **5 项全过**（`SYNTAX` / `MISSING` / `DUPLICATE literal ids` / `UNDECLARED` / `ESCAPE-GATE`）—— 新增的 `st-persona-tpl-fold` / `-toggle` / `-body` 三个 id 都过了重复检查 |
| `persona-verify.js` | **155 → 177 条，177 通过 / 0 失败**（新增 22 条） |
| `_reverse13.js` | **全部达标**；新增 **R6 探针**（打断 `stPersonaTplToggle`）⇒ 预期 6 条红、**实测恰好 6 条**、16 条对照组一条不红、还原后逐字节一致 |
| 本地整跑 `run-all.js` | **16 套 / 2 234 通过 / 0 失败** —— 跟 README §7 的「共 2 234 条」**对得上**（我从表格自己枚举求和，也是 2212 → 2234） |
| 外部 8 层 | **7 套断言 1 631 通过 / 0 失败** + `static_check` ✅ |
| `_audit-anchors.js` | 两张表**全漂** ⇒ 重取后**归零**（见下） |
| 产品 | `1 493 542 字节 / 29 213 行 / CRLF 29 212 / 孤立 LF 0 / sha1 d0bf0d34afef…` |

### 顺手改掉一条**逻辑上恒真**的对照

原来写的是 `check('（对照）状态提示**不在**模板前面', order.status < order.fold, false)` —— 那是主断言的**反命题**，恒真，等于没对照。真正要防的是 **`indexOf` 找不到时返回 `-1`**：只要 `fold` 是 `-1`，任何 `status` 都「大于」它 ⇒ 主断言天然成立。已改成「**两个节点都在顺序表里找到了**」。

### 锚点表：两种位移，而且**有个数本来就是错的**

`_audit-anchors.js` 一次报 **20 行**对不上（§2 九行 + §3 八行 + 多行横幅那串）：

| 范围 | 位移 | 原因 |
|---|---|---|
| §2 整表（`5193…5577`） | **+58** | 折叠块 CSS（~60 行）+ retro 补丁（3 行） |
| §3 的 `2840…3641` | **+55** | 只吃到折叠块的 CSS |
| §3 的 `3839` 之后 | **+58** | 再多 3 行 = retro 补丁（插在 3721 附近） |

⚠ **同一张表里两种位移** —— 按加法推必错（这是第三次实测到同一现象）。
⚠⚠ **§3 那串「多行大横幅」里的 `2355` 本来就是错的**（真值 **2382**）：它不是「这一轮漂的」，是**上一轮就抄错了**、而且一直没人发现 ⇒ **这张表不是「算一次就永远对」，错的数会被反复抄下去**。已在 README 里写明并留痕。
⚠ 重取方法：`_audit-anchors.js` 指出「哪一行不是横幅」，但**它给的「最近的上一个横幅」不是答案**（真值在那个横幅**之后**）—— 要**现 `grep`** 横幅正文取真值。

### 一个「差点漏掉」的收尾

产品改了就必须 `cp saki.html index.html`（**站点发布的是 `index.html`**）。已做 + `cmp` 逐字节一致。

### 文档同步

- `markdown/README.md`：§1 产品判据（`1 493 542 / 29 213 / d0bf0d34afef…`）、§2 锚点表（+58）、§3 锚点表（+55 / +58）、多行横幅那串（含 `2355`→`2382` 的留痕）、§7 persona 那行（155 → 177 + 新行为描述）、§7 总数（2 212 → 2 234）。
- `markdown/04-card-editor.md` §十六：新增第 4 条设计决定（折叠 + 为什么不落盘 + 为什么不 `stRerender`）、状态表加 `personaTplOpen`、函数表加两个、DOM id 补折叠那三个并写明标题**必须保留 `.st-sub`**。
- `markdown/CHANGELOG.md`：新增「**2026-09-23（第五段）**」（最新在上）。
- 五份镜像 `cmp` 逐字节一致。

